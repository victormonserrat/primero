a) int *px; //Declaración del puntero "*px" de tipo int*.
b) float a, b; //Declaración de las variables "a" y "b" de tipo float.
c) float *pa, *pb; //Declaración de los punteros "*pa" y "*pb" de tipo float*.
d) float a = ­-0.167; //Declaración de la variable "a" de tipo float e iniciación de la misma con valor de -0.167.
e) float *pa = &a; //Declaración del puntero "*pa" de tipo float* y asignación de la dirección de memoria de la variable "a" a este.
f) char c1, c2, c3; //Declaración de las cadenas de caracteres "c1", "c2", y "c3".
g) char *pc1, *pc2, *pc3 = &c1 //Declaración de los punteros "*pc1", "*pc2" y "*pc3" de tipo char* y asignación a este último de la dirección de memoria de la cadena c1.
