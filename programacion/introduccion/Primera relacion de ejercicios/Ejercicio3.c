#include <stdio.h>

int main()
{

	printf("El tamaño en Bytes de un char es %i, el de un int es %i, el de un long es %i, el de un float es %i y el de un double es %i.\n\n", sizeof(char), sizeof(int), sizeof(long), sizeof(float), sizeof(double));

	return(0);
}
